# Backend origin for production builds (e.g. the Render URL).
# Leave UNSET for local dev — the Vite dev proxy handles /api.
# Set this in the Vercel project (Environment Variables) to your backend URL:
#   VITE_API_BASE=https://luad-backend.onrender.com
VITE_API_BASE=
